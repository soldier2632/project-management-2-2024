mixin form-search(keyword)
  form(id="form-search")
    .input-group
      input(
        type="text"
        class="form-control"
        placeholder="Nhập từ khóa"
        name="keyword"
        value=keyword
        )
      .input-group-append
         button( 
           class="btn btn-success"
           type="submit"
           ) Tìm kiếm 
